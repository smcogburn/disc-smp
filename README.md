# DiscSMP (minimal)
KNICKS IN 5 (lets go)
Build

```bash
./gradlew build
```

Install

- Drop the produced jar from `build/libs` into your Paper server `plugins` folder.
- Start the server and run `/givedisc` to receive the demo disc.
